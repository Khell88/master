krytek_data:
    resource: "@KrytekDataBundle/Controller/"
    type:     annotation
    prefix:   /sistema

krytek_login:
    resource: "@KrytekLoginBundle/Controller/"
    type:     annotation
    prefix:   /

app:
    resource: "@AppBundle/Controller/"
    type:     annotation

logout:
    path: /logout
