{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bolsa edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('bolsa_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

{% block javascripts %}
    <script type="text/javascript">
        var doc;
        doc = $(document);
        doc.ready(inicio());
        function inicio() {
            var fecha = $("#krytek_databundle_bolsa_fechaDonacion");
            fecha.datepicker();
            var fecha = $("#krytek_databundle_bolsa_fechaVencimiento");
            fecha.datepicker();
        }
    </script>
{% endblock %}