{% extends 'base.html.twig' %}

{% block body %}
    <h1>Motivotransfusion</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ motivoTransfusion.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ motivoTransfusion.descripcion }}</td>
            </tr>
            <tr>
                <th>Componente Asociado</th>
                <td>{{ motivoTransfusion.componente }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('motivotransfusion_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('motivotransfusion_edit', { 'id': motivoTransfusion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
